params:
- prefix: naming prefix for the containers
containers:
- name: ${prefix}-sidecar0
  image: busybox:latest
  tty: true
  stdin: true
- name: ${prefix}-sidecar1
  image: busybox:latest
  tty: true
  stdin: true
