imports:
- affinity: ./affinity.yaml
- containers: ./containers.yaml
pod:
  labels:
    app: backend
  name: backend
  affinity: ${affinity}
  containers:
  - name: app-container
    image: busybox:latest
    tty: true
    stdin: true
  - ${containers...}
