deployment:
  labels:
    app: example
  name: example-deployment
  replicas: 3
  template:
    containers:
    - expose:
      - port_map: "80"
        protocol: TCP
      image: example/example:latest
      name: example-app
    labels:
      app: example
  version: extensions/v1beta1
