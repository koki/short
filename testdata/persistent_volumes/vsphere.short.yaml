persistent_volume:
  annotations:
    annotationKey: annotationValue
  claim:
    name: claimName
    namespace: claimNamespace
  cluster: cluster
  fs: ext4
  labels:
    labelKey: labelValue
  modes: rw-once
  mount_opts: option 1,option 2,option 3
  name: vol-name
  namespace: namespace
  policy:
    id: policy-id
    name: policy-name
  reclaim: recycle
  storage: 10Gi
  storage_class: storageClass
  version: v1
  vol_id: '[datastore1] volumes/myDisk'
  vol_type: vsphere
